name: deploy

on: push

jobs:
    first_job: 
        runs-on: ubuntu-latest

        steps:
          - name: Print variable
            run: echo "${{ secrets.MY_SECRET }}"

          - name: Setup
            uses: actions/setup-node@v4
            with:
              node-version: '22'
